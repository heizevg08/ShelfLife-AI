{
  "name": "@shelflifeai/client",
  "version": "1.0.0",
  "private": true,
  "main": "expo-router/entry",
  "scripts": {
    "web": "expo start --web",
    "client": "npm run start --workspace client",
    "server": "npm run start --workspace server",
    "dev": "npm run client",
    "typecheck": "tsc --noEmit",
    "build:web": "npm run build:web --workspace client"
  }
}
